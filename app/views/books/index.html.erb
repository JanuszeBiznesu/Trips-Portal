<% provide(:title, 'All books') %>
<h1>All users</h1>
<div class="list">
<%= will_paginate %>

<ul class="books">
  <% @books.each do |book| %>
    <%= render book %>
  <% end %>
</ul>

<%= will_paginate %>
</div>